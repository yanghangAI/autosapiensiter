## Design review — 2026-04-22

APPROVED. Pelvis-token FiLM site after `joints_out(decoded)` and before `depth_out`/`uv_out` is explicit; body pathway preserved unmodulated. Shape handling correct (no unsqueeze needed). Invariants preserved.
